<Window x:Class="PS7ScriptDesk.Shell.StoreUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PS7 ScriptDesk Update"
        Width="700"
        Height="480"
        MinWidth="680"
        MinHeight="440"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanMinimize"
        Background="#F8FBFF"
        Foreground="#172033"
        FontFamily="Segoe UI Variable Display, Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="AltSurfaceBrush" Color="#EEF4FB" />
        <SolidColorBrush x:Key="BorderBrush" Color="#C8D5E5" />
        <SolidColorBrush x:Key="PrimaryButtonBrush" Color="#1F2937" />
        <SolidColorBrush x:Key="PrimaryButtonHoverBrush" Color="#334155" />
        <SolidColorBrush x:Key="DisabledButtonBrush" Color="#CBD5E1" />
        <SolidColorBrush x:Key="DisabledButtonBorderBrush" Color="#94A3B8" />
        <SolidColorBrush x:Key="DisabledButtonForegroundBrush" Color="#334155" />

        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#475569" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource PrimaryButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#111827" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource DisabledButtonBrush}" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource DisabledButtonBorderBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource DisabledButtonForegroundBrush}" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock"
                   Grid.Row="0"
                   FontSize="20"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap" />

        <Border Grid.Row="1"
                Margin="0,14,0,0"
                Padding="14"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="10">
            <TextBlock x:Name="MessageTextBlock"
                       TextWrapping="Wrap"
                       LineHeight="20" />
        </Border>

        <ScrollViewer Grid.Row="2"
                      Margin="0,16,0,0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Border Background="{StaticResource AltSurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12">
                    <StackPanel>
                        <TextBlock Text="Updates detected"
                                   FontWeight="SemiBold" />
                        <ItemsControl x:Name="UpdatesItemsControl"
                                      Margin="0,10,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,6" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Margin="0,14,0,0"
                        Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12">
                    <StackPanel>
                        <TextBlock Text="Status"
                                   FontWeight="SemiBold" />
                        <TextBlock x:Name="StatusMessageTextBlock"
                                   Margin="0,10,0,0"
                                   TextWrapping="Wrap"
                                   LineHeight="20" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <UniformGrid Grid.Row="3"
                     Rows="3"
                     Columns="2"
                     Margin="0,18,0,0">
            <Button x:Name="InstallNowButton"
                    Content="Install Update Now"
                    Margin="0,0,7,7"
                    Click="InstallNowButton_Click" />
            <Button Content="Open Microsoft Store / manual instructions"
                    Margin="7,0,0,7"
                    Click="OpenStoreButton_Click" />
            <Button Content="Open Logs Folder"
                    Margin="0,7,7,7"
                    Click="OpenLogsFolderButton_Click" />
            <Button Content="Copy Logs Path"
                    Margin="7,7,0,7"
                    Click="CopyLogsPathButton_Click" />
            <Button x:Name="CloseOrExitButton"
                    Margin="0,7,7,0"
                    Click="CloseOrExitButton_Click" />
            <Border Margin="7,7,0,0"
                    Background="Transparent" />
        </UniformGrid>
    </Grid>
</Window>
